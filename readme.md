This app creates an LLM based chatbot using OpenAI API, streamlit and Langchain.

Installations:
1. pip install langchain
2. pip install streamlit
3. pip install lanchain-openai
4. pip install python-dotenv